function y = lagrangeaula(X,Y,x)
    if(length(X)~= length(Y))
            if(min(X)<= x && max(X)>= x)
            for i=0: n
            l(i)=1;
            for j = 1:n
               if j = i

            end
            P = P + Y(i)*l(i)*(x);
            end
            else
                disp('Extrapolação')
                return
    
            end
    else
        disp('Vetores com comprimentos diferentes')
    end
end




